# 開発ワークフロー

このリポジトリでコード、設定、テスト、ドキュメントを変更する際は、次のルールを守ること。

1. `main` ブランチ上で変更を加えない。作業開始前に、最新の `main` を起点とした目的が分かる作業ブランチを作成して切り替える。
2. 変更をコミットし、マージ先を `main` とするプルリクエストを作成する。
3. プルリクエストを作成した後は、ユーザーから明示的に依頼されない限りマージしない。自動マージも有効化しない。
4. `main` に未コミットの変更がある場合は、その変更を移動、退避、破棄せず、作業の進め方をユーザーに確認する。
5. 要件、仕様、影響範囲、実装方針などに不明点があり、判断によって結果が変わり得る場合は、推測で進めずユーザーに確認してから作業する。
