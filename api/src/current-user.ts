/**
 * いまログインしているユーザー。
 *
 * ログインの仕組みがまだ無いので固定値です。アカウント切り替えの UI を
 * 用意したら、ここをセッションからの解決に差し替えます。
 *
 * ★ユーザーの特定はサーバー側で完結させること。クライアントから `user_id` を
 *   受け取る形にすると、切り替え時にクライアントも直すことになり、
 *   何より他人の回答を書き換えられてしまいます。
 */
export const CURRENT_USER_ID = "test_user1";
